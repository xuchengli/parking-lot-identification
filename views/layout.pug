doctype html
html
    head
        title Parking Lot Identification
    body
        block content
        if env == "development"
            script(src="/bundle.js")
            script(src="/reload/reload.js")