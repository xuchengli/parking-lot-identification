doctype html
html
    head
        base(href=context + "/")
        title(data-i18n="parking-lot-management")
    body.uk-height-viewport
        block content
        if env == "development"
            script(src="/bundle.js")
            script(src="/reload/reload.js")